/**
 * ThemeToggle Interface Definitions
 * Type definitions for the ThemeToggle component.
 */

/**
 * ThemeToggleProps
 * Props for the ThemeToggle component.
 * @property showLabel - Whether to display the label text inside the button
 * @property className - Optional additional CSS classes
 */
export type ThemeToggleProps = {
  showLabel?: boolean;
  className?: string;
};
